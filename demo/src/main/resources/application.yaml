spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_security_demo
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080
  servlet:
    context-path: /identity
logging:
  level:
    org.hibernate.SQL: debug

